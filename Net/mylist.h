/*
#pragma once

struct Node* list_create(void* val);

// Add a node to the end of the list
// Returns the first node of the list
void list_add(void* val);

// Remove the specified address' NEXT node
void list_remove(void* addr);

void list_destroy();
*/