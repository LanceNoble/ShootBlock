#include <stdio.h>
//#include <Windows.h>

//#include "server.h"
#include "mem.h"

int main() {
	/*
	server_create();
	server_start();

	while (!(GetAsyncKeyState(VK_END) & 0x01)) {
		server_greet();
		server_sync();
	}

	server_stop();
	server_destroy();
	*/
}